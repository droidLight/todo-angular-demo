import { DoneDirective } from './done.directive';

describe('DoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DoneDirective();
    expect(directive).toBeTruthy();
  });
});
